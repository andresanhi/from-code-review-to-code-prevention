name: flutter_linters_demo
description: "A new Flutter project."

publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  #
  # 🌍 NIVEL TERRÍCOLA — activo por defecto. No requiere nada más.
  flutter_lints: ^6.0.0

  # 🤖 NIVEL ANDROIDE — descomenta estas 2 líneas y corre `flutter pub get`.
  # Luego, en analysis_options.yaml: cambia el `include` a very_good_analysis
  # y descomenta "- dart_code_linter" dentro de `analyzer: plugins:`.
  # Nota: se fija en ^10.2.0 (no ^10.3.0) porque very_good_analysis 10.3.x
  # requiere Dart SDK >=3.12.0 y este proyecto usa ^3.11.5.
  # very_good_analysis: ^10.2.0
  # dart_code_linter: ^4.1.8

  # ⚡ NIVEL SAIYAJIN — descomenta estas líneas y corre `flutter pub get`.
  # Luego, en analysis_options.yaml, descomenta "- custom_lint" dentro de
  # `analyzer: plugins:`.
  # custom_lint: ^0.6.0
  # team_lints:
  #   path: lib/packages/team_lints

flutter:
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
