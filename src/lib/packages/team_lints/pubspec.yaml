name: team_lints
description: Custom lint rules for the team — repository naming & hardcoded colors.
version: 0.0.1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  analyzer: ">=6.4.1 <8.0.0"
  custom_lint_builder: ^0.6.0

dev_dependencies:
  custom_lint: ^0.6.0
  test: ^1.24.0
